server:
  port: 8080
spring:
  application:
    name: whim
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
      enabled: true
  # 开发环境
  profiles:
    active: ${PROFILES_ACTIVE:dev}